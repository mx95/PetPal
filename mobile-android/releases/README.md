# Android App Bundles

| File | Status |
|------|--------|
| `petpal-1.0.0-release.aab` | Built by GitHub Actions or `scripts/build-release.ps1` |

If missing, trigger **Actions → Build Android Release** or build locally (see [../README.md](../README.md)).

Upload the `.aab` to Google Play Console → **Production → Create release**.
