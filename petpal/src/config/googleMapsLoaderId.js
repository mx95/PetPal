/**
 * Single id for all `useJsApiLoader` / LoadScript in the app. Different ids can make
 * @react-google-maps/api load the Maps JavaScript API more than once and throw at runtime.
 */
export const GOOGLE_MAPS_LOADER_ID = 'petpal-google-maps';
