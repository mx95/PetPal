.DS_Store
node_modules/

# Local utilities / downloads
ngrok/
*.zip

# Tracker TCP server — live GPS database (never commit)
tracker-tcp-server/node_modules/
tracker-tcp-server/data/*.sqlite
tracker-tcp-server/data/*.sqlite-*
tracker-tcp-server/data/backups/
# Production DB lives outside the repo on the server (/var/lib/petpal/petpal.sqlite)
